{
  "name": "biseo",
  "private": true,
  "version": "1.0.0",
  "repository": "https://github.com/sparcs-kaist/biseo.git",
  "license": "MIT",
  "scripts": {
    "prepare": "husky install",
    "dev": "dotenv lerna run dev",
    "dev:db": "dotenv lerna run dev:db",
    "dev:db:migrate": "dotenv lerna run dev:db:migrate"
  },
  "workspaces": [
    "server",
    "client",
    "interface"
  ],
  "devDependencies": {
    "@commitlint/cli": "^17.4.2",
    "@commitlint/config-conventional": "^17.4.2",
    "dotenv-cli": "^7.2.1",
    "husky": "^8.0.3",
    "lerna": "^6.6.1",
    "prettier": "^2.8.7"
  }
}
